import React, { Component } from "react";
import { SideEvents } from "javascripts/components";

class SideEvent extends Component {
  render() {
    return (
      <SideEvents />
    );
  }
};

export default SideEvents;
